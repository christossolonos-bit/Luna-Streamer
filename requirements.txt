# >=0.5.0 ships the `think` kwarg needed to suppress qwen3/deepseek-r1
# chain-of-thought; older versions silently drop the kwarg and the model
# spends every num_predict token "thinking" with an empty visible reply.
ollama>=0.5.0
twitchio>=2.10,<3
python-dotenv>=1.0.0
aiohttp>=3.9.0
# Speaker verification reads WAVs via soundfile (faster than librosa for the
# already-normalised 16k mono clips). librosa still computes the MFCC features.
soundfile>=0.12.0

# STT: local faster-whisper (optional GPU / CUDA build of ctranslate2).
faster-whisper>=1.0.0
# Viewer mic male-voice gate (LUNA_VOICE_GATE_MALE_ONLY=1): pitch estimate via pyin.
librosa>=0.10.0

# YouTube integration: !play (audio resolver/download) + !explain (transcript).
yt-dlp>=2024.4.9
youtube-transcript-api>=0.6.2

# Optional: Discord music bot for voice-channel playback (DISCORD_TOKEN required).
# The [voice] extra pulls PyNaCl. FFmpeg must be in PATH (already required for STT).
discord.py[voice]>=2.3.0

# Optional Luna voice (LUNA_TTS=1): Edge TTS backend (Python 3.13 friendly).
edge-tts>=6.1.0
pydub>=0.25.1
# Optional: Playwright posts new observe uploads to X / Facebook (saved browser sessions).
# pip install playwright && python -m playwright install chrome
# Login helper (run from repo root): python scripts/social_playwright_login.py https://x.com D:/x.json
playwright>=1.49.0
# Optional: read YouTube Live chat into Luna's viewer (LUNA_YOUTUBE_LIVE_CHAT=1); replies are UI-only.
pytchat>=0.5.0
